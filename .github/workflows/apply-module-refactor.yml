name: Apply module structure refactor

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Migration already applied on refactor/package-by-module. Keep this workflow manual-only for audit/replay."
